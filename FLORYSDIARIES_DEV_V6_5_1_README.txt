FlorysDiaries DEV – v6.5.1 Stabilitäts- und Testfix

Enthaltene Korrekturen:
- Reise-Hero ohne Überlauf auf schmalen Displays und bei größerer Schrift
- Detail-Hero ohne horizontale und vertikale RenderFlex-Overflows
- Datensicherheits-Sperrbildschirm mit Wiederholen- und Backup-Aktion
- Tests an die aktuelle Navigation Home / Reisen / Karte / Momente angepasst
- Statistik-Test an die aktuelle Überschrift „Statistiken“ angepasst
- Starttest ohne zu frühe Textprüfung
- README.md, CHANGELOG.md und .gitignore im Projektstamm ergänzt

Alle Dateien anhand ihrer Pfade direkt in den Projektordner kopieren und vorhandene Dateien ersetzen.
README.md, CHANGELOG.md und .gitignore müssen anschließend direkt neben pubspec.yaml liegen.
